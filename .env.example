# Example environment variables for tests
# Copy this file to `.env` and fill with real credentials (do NOT commit `.env`)

# Test user credentials used by tests/test-utils/fixtures.ts
TEST_USER_EMAIL=tester+testowy@gmail.com
TEST_USER_PASSWORD=test123
# Optional: override base URL used by fixtures if different from playwright.config.ts
# Provide a full URL (including scheme)
BASE_URL=https://bearstore-testsite.smartbear.com/
GOREST_TOKEN=

